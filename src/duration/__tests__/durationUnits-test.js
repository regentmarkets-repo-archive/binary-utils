import durationUnits from '../durationUnits';

describe('durationUnit', () => {
    it('should return durationUnits of type array', () => {
        expect(durationUnits).toBeInstanceOf(Array);
    })
});
